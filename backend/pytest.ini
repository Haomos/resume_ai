[pytest]
markers =
    perf: performance / benchmark tests (slow; opt-in via -m perf)
    realperf: real-LLM perf tests against Ollama / Kimi (very slow; opt-in via -m realperf + RUN_REAL_LLM_PERF=1)
addopts = -m "not perf and not realperf"
