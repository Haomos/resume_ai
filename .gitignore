# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Conda
*.conda
*.env

# Data & models (local-only)
data/
models_cache/
faiss_index/
uploads/
*.db
*.db.*
*.sqlite3
*.bak

# Secrets
.env
.env.*
!.env.example
!.env.prod.example
secrets/

# Node
node_modules/
*.log
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/

# Docker
*.pid

# Claude / Ruflo internal
.claude/
.claude-flow/
.swarm/
.scratch/

# Private fixtures (real resumes / personal test data — NEVER commit)
MEMORY/private/
REFACTOR_TECH_SPEC.md
MEMORY.md

# Git history backups (created by filter-repo / before rewrites)
.git.backup*/

# Perf benchmarks: results JSON is regenerated on every run.
# The file is kept in repo as historical snapshot via initial commit;
# subsequent local runs are ignored.
backend/perf/batch_perf.json
backend/perf/batch_perf_real.json

# Debug snapshots: one-off SQL dumps for inspecting DB state.
# These are not test fixtures (tests use SQLAlchemy fixtures); they're
# disposable debug artifacts. Never commit.
analyses_dump*.json
*_dump.json
