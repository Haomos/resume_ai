# 前端环境变量样板。复制为 .env.local 后修改即可被 Vite 注入。
# 注意 Vite 只暴露 VITE_ 前缀的变量到客户端代码。

# 后端 API 基础地址。默认 127.0.0.1:8000，与 backend uvicorn 绑定一致。
# 如部署到其他主机或反向代理后，改成完整 URL（含协议）。
VITE_API_BASE_URL=http://127.0.0.1:8000
